{
    "sqltools.useNodeRuntime": true,
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "Tarefas",
            "database": "${workspaceFolder:To-do-List}/tarefas.db"
        }
    ]
}